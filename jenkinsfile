pipeline {
    agent any 
    stages {
        stage('Gradle build') { 
            steps {
                dir ('./birdmeal') {
                    sh 'chmod +x gradlew'
                    sh './gradlew clean bootJar'
                }
            }
        }
        stage('Dockerfile') { 
            steps {
                // 
            }
        }
    }
}