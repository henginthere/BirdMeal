pipeline {
    agent any 
    stages {
        stage('Gradle build') { 
            steps {
                dir ('./birdmeal') {
                    sh 'chmod +x gradlew'
                }
            }
        }
        stage('Dockerfile') { 
            steps {
                // 
            }
        }
    }
}